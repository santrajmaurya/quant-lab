{
  "name": "quantlab-frontend",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "prefix": "app",
  "sourceRoot": "apps/quantlab-frontend/src",
  "tags": [],
  "targets": {
    "extract-i18n": {
      "executor": "@angular-devkit/build-angular:extract-i18n",
      "options": {
        "buildTarget": "quantlab-frontend:build"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    },
    "serve-static": {
      "continuous": true,
      "executor": "@nx/web:file-server",
      "options": {
        "buildTarget": "quantlab-frontend:build",
        "port": 4200,
        "spa": true
      }
    },
    "serve-ssr": {
      "continuous": true,
      "executor": "@angular-devkit/build-angular:ssr-dev-server",
      "defaultConfiguration": "development",
      "configurations": {
        "development": {
          "browserTarget": "quantlab-frontend:build:development",
          "serverTarget": "quantlab-frontend:server:development"
        },
        "production": {
          "browserTarget": "quantlab-frontend:build:production",
          "serverTarget": "quantlab-frontend:server:production"
        }
      }
    }
  }
}
